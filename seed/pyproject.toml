[tool.poetry]
name = "seed"
version = "0.1.0"
description = "One-shot database seeder"
authors = ["Bezketnyky <boss@email.com>"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.10,<4.0"
shared = { path = "../shared-lib", develop = true }

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
